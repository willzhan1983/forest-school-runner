# 森林学校跑酷游戏 · 交付概览

## 交付物
- **文件**：`/Users/mac/WorkBuddy/2026-08-28-23-20-13/forest-school-runner.html`
- **类型**：单文件 HTML5 Canvas 游戏
- **大小**：约 300 KB（主要为两个角色立绘的 base64 内联图片）
- **依赖**：零外部依赖，双击用浏览器打开即可运行

## 角色
| 角色 | 中文名 | 英文名 | 形象 | 专属技能 |
|---|---|---|---|---|
| 猫 | 钱钱 | Fuzzy | 灰白猫 | **冲刺** — 向前猛冲，撞碎障碍并短暂无敌（CD 3 秒） |
| 猫头鹰 | 涂涂 | Doodle | 黄绿猫头鹰 | **滑翔** — 空中二段跳，长按展翅缓降 |

- 已把你提供的角色图（`/Users/mac/Desktop/涞儿的任务/游戏/Codex 图像 2026年8月29日 01_08_43.png`）抠出两个角色主体，去掉底部英文标签，内嵌进 HTML，**无需任何外部图片文件**。
- 仍支持在界面内点击上传本地图片替换角色，图片存 localStorage（`fsr_pic_cat` / `fsr_pic_owl`）。
- 也可直接用序列帧：`CHARACTERS.cat.sheet = { src:'cat_sheet.png', cols:6, rowRun:0, rowJump:0, ... }`。

## 已实现功能
- **双角色系统**：开始页选择，局内可切换。
- **三主题循环关卡**：森林 → 教室 → 树屋，视差背景，障碍/平台/收集物随机生成，速度与难度随距离递增。
- **完整跑酷玩法**：自动奔跑、跳跃、滑铲、二段跳、技能、收集橡果/书本计分、三格生命、碰撞扣血、失败结算与重开。
- **界面**：开始页角色选择、游戏中 HUD（分数/生命/距离/收集/道具状态/暂停/音乐）、失败结算页（最高分存 localStorage，整数显示）。
- **输入适配**：键盘（空格/↑ 跳、↓ 滑铲、D/Shift 技能、P 暂停）+ 触屏（点击跳、下滑滑铲、长按滑翔、右下角技能键），窗口自适应缩放，竖屏不黑屏（顶部提示条可关闭）。
- **最高分**：`fsr_best`，结算页按整数显示。

## 本轮新增优化（背景 / 道具 / 画面 / 音乐）

### 1. 玩法道具
| 道具 | 效果 | 持续 |
|---|---|---|
| 护盾 🛡 | 抵挡一次伤害，破碎时蓝色冲击环 | 直到被消耗 |
| 磁铁 | 自动吸附半径约 160px 内的橡果/书本 | 约 8.7 秒 |
| 双倍分 | 所有得分翻倍，金色虚线光环 | 约 10.3 秒 |

- 道具随机飘在场景中，吃到有上升音阶提示音。
- HUD 左侧显示道具状态图标：圆形底 + 剩余时间进度环 + 层数角标。
- 磁铁有可见的紫色吸附范围圈。

### 2. 背景音乐 + 昼夜循环
- **BGM**：Web Audio 实时合成的三层循环编排（贝斯 + 主旋律 + 轻打击），C-G-Am-F 和声进行，16 步 / 6.7 秒一循环，带 0.7 秒淡入、0.35 秒淡出。
- **昼夜**：50 秒一个循环 — 黎明 → 白天 → 黄昏 → 夜晚 → 黎明。夜晚降八度、音量降 45%、打击减弱，画面叠加深蓝压暗层并出现月亮。
- HUD 左上角**音符按钮**可开关音乐，选择记在 `localStorage` 的 `fsr_music`。
- 游戏开始自动播放，暂停 / 结算自动停止，恢复自动续播。

### 3. 画面精致度
- 飘落物：森林落树叶、教室飘粉笔灰、树屋落花瓣。
- 地面草丛、障碍底部椭圆阴影、收集物外发光、阳光光束。

## 验证结果（Puppeteer + Chrome 实测）
| 检查项 | 结果 |
|---|---|
| 页面 / 控制台错误 | 无 |
| BGM 排程推进 | 7 → 10 音符，音量 0.85 ✅ |
| 昼夜亮度对比 | 白天 159 → 夜晚 99（暗 37%），像素差异均值 60.9 ✅ |
| 音乐按钮开关 | on→off→on 正常，状态持久化 ✅ |
| 暂停 / 恢复 / 结算 | 音乐自动停 / 续 / 停 ✅ |
| 护盾抵挡伤害 | 撞障碍后 2→1 层，生命仍为 3 ✅ |
| 磁铁 / 双倍分 | 生效并按时衰减 ✅ |
| 三个主题场景渲染 | 森林 / 教室 / 树屋均正常 ✅ |

## 二次修改指引
打开 HTML，搜索对应关键词即可改：
- 角色配色 → 搜 `colors:{fur:`（猫）或 `colors:{top:`（猫头鹰）
- 场景配色 → 搜 `var THEMES`
- 物理手感 → 搜 `GRAVITY` / `MAX_FALL` / `speed:5.6`
- 道具时长 → 搜 `POWERUP_META`
- BGM → 搜 `MUSIC_MEL`（主旋律）与 `MUSIC_BASS`（贝斯），改音符频率或 `beat:0.42`（速度）
- 昼夜快慢 → 搜 `dayPhase`（`% 50` 即 50 秒一循环）
